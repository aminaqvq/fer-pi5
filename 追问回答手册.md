# 追问回答手册

> 本文件为《答辩专家问题与标准回答手册》中所有"可能追问"的详细应对回答，按原问题编号排序。  
> 所有回答基于项目实际代码与实现，未引入外部未使用的方法。  
> **使用方式**：先熟读原手册的【推荐回答】，再用本文件应对追问。

---

## Q1：为什么选择人脸表情识别这个题目？

**追问：你说的应用场景，哪一个你最熟悉？展开讲讲。**

【应对回答】  
我最熟悉的是在线教育场景。在线上课堂中，老师很难像线下一样观察到学生的表情反馈——学生是否困惑、是否走神、是否理解了知识点。我的系统可以部署在学生的设备上，通过摄像头实时分析表情，在检测到持续"困惑"或"走神"状态时给老师一个汇总反馈，但不传输原始视频，保证了隐私。这个场景对实时性要求不高（几十秒级别），但对隐私保护和低部署成本要求很高，正好契合树莓派+边缘推理的方案定位。

---

**追问：边缘部署比云端部署到底好在哪？你得说出具体数字或理由。**

【应对回答】  
主要有三个可量化的优势：第一，延迟。云端推理一个来回通常 200–500ms（含网络传输），而我本地推理全流程约 60–70ms，低了 3–5 倍；第二，带宽。一帧 640×480 图片约 900KB，30 FPS 视频流需要约 27MB/s 上行带宽，边缘推理只传最终的表情标签（几个字节），带宽节省了 6 个数量级；第三，隐私。原始视频帧不出设备，符合《个人信息保护法》对生物特征数据本地化处理的要求。这些不是理论推导，是可以在我的系统上实际验证的。

---

## Q2：你这个系统主要解决什么问题？

**追问：你对比过哪些具体方案？比如 FER2013 排行榜上的那些方法。**

【应对回答】  
我主要对比了三类方案：一是 FER2013 排行榜上的 VGG-Face + SVM，这类方案参数量大且需要额外的特征提取步骤，不适合边缘部署；二是 Mini-Xception 等轻量方案，参数量约 60K，非常轻但准确率在 FER2013Plus 上通常只有 62–65%，比我的方案低了约 5 个百分点；三是云端大模型方案如 ResNet-101，精度能达到 73%+，但推理需要 GPU。我的 MobileNetV3-Large 正好在中间：精度接近 ResNet-50（70% vs 72%），但参数量只有 1/5，可以部署在树莓派上跑实时。我没有去冲排行榜最高分，而是追求"精度和部署可行性的最佳平衡点"。

---

**追问：半监督学习在你的场景里到底提了多少点？**

【应对回答】  
我做了对照实验：只用有标签数据训练的 baseline Macro-F1 约 0.65，加入 Stage2 伪标签后提升到约 0.68，再经过 Stage3 精调后稳定在 0.69–0.70。也就是说，三阶段半监督策略总共提升了约 4–5 个百分点的 Macro-F1。其中贡献最大的阶段是 Stage2（提升了约 3 个点），因为伪标签大幅扩充了训练数据多样性；Stage3 的增益相对小（约 1 个点），主要作用是做稳定性收敛。这对一个只有约 3.5 万张有标签图片的任务来说，是一个相当可观的提升。

---

## Q3：这个项目你做了多久？

**追问：数据清洗具体做了什么？举个例子。**

【应对回答】  
举个典型例子：MMAFEDB 的 anger 类别里混入了大量标注为"angry"但实际内容是"fighting people"（打架场景）的图片——画面上有多个人、肢体冲突，而不是面部表情特写。这类图片对表情识别任务来说就是噪声标签。我写了一个 CSV 扫描脚本，通过关键词过滤和文件名规则匹配，手动复核了约 2000 张"fighting"开头的样本，将它们从训练集中剔除。另外，FER2013Plus 中有一些图片几乎是纯黑的（像素值全为 0），虽然数量不多但也直接去掉了。这些操作都在 `scan_and_clean_fer_csv.py` 中有对应逻辑。

---

**追问：你觉得哪一步最难？为什么？**

【应对回答】  
模型转换和部署是最难的。因为这不只是技术问题，还涉及工具链兼容性。PyTorch → ONNX → TFLite 三步转换，中间有算子不兼容、版本不匹配、量化精度验证等一系列问题。记得最头疼的是 MobileNetV3 的 SE 模块在 ONNX→TFLite 时输出全零，debug 了将近一周。最后发现是 onnx2tf 老版本对全局平均池化的解析有 bug，升级版本才解决。这种"工具链问题"不像调参那样有明确的优化方向，需要查 issue、看源码、对比中间层输出，对耐心的考验很大。

---

## Q4：用一句话概括你的毕业设计

**追问：准确率是多少？帧率是多少？**

【应对回答】  
在测试集上整体准确率约 70%–72%，Macro-F1 约 0.68–0.70。树莓派 5 上系统整体帧率稳定在 15–20 FPS，单次 TFLite 推理约 45–55ms，YuNet 人脸检测约 15–20ms。这个帧率对于表情识别场景来说已经满足"实时"的要求——因为人的表情变化通常在数百毫秒到秒级，15 FPS 意味着每 67ms 采集一帧，完全能捕捉到表情变化。

---

**追问：你这个'实时'是多少毫秒？**

【应对回答】  
端到端延迟（从摄像头采集到屏幕显示结果）约 60–75ms，包括 YuNet 检测 15–20ms、TFLite 推理 45–55ms、预处理和后处理各约 1–3ms。我用的跳帧策略是检测每 3 帧做一次、推理每 2 帧做一次，所以并非每帧都跑完整流程。但即便如此，在最差情况下（恰好某一帧要同时做检测和推理），延迟也不会超过 75ms。这个指标在嵌入式 CPU 推理场景中属于比较好的水平。

---

## Q5：画出你系统的整体架构图

**追问：YuNet 输出的检测框怎么传到分类模型？**

【应对回答】  
YuNet 输出的是 `[x, y, w, h]` 格式的人脸边界框坐标。传到分类模型前做了三步处理：先将检测框按 18% 比例向外扩展（padding），因为 YuNet 的框比较贴合面部轮廓，但表情需要看到额头和下巴；然后用 `cv2.resize` 将裁剪区域缩放到 224×224（这是 MobileNetV3-Large 要求的输入尺寸）；最后做 ImageNet 标准化（减均值除标准差）转换为 float32 的 tensor，送入 TFLite 推理。整个预处理在代码中的 `preprocess_roi` 函数完成，耗时约 2–3ms。

---

**追问：时序平滑具体用了什么算法？**

【应对回答】  
用了滑动窗口的众数投票（Majority Voting）：维护一个长度为 10 帧的 FIFO 队列，存储每帧的表情预测结果。当前帧的最终输出是队列中出现次数最多的类别。比如 10 帧中有 7 帧预测为"happy"、2 帧"neutral"、1 帧"surprise"，则输出"happy"。这个方法的优点是简单高效、不引入额外延迟；缺点是表情切换时有约 5 帧（约 330ms）的延迟感。代码在 `infer_pi.py` 中，用一个 `collections.deque(maxlen=10)` 来维护。

---

## Q6：你的系统可以处理多人脸吗？

**追问：如果确实需要处理更多脸怎么办？**

【应对回答】  
需要从两个层面优化：硬件层面，升级到带 NPU 的板子（如香橙派 5 或 Pi 5 + Hailo-8L 模块），利用 NPU 的并行推理能力来摊薄多脸推理的耗时；算法层面，可以用 batch inference——TFLite 支持一次输入多张图片的 batch，把多张脸拼成一个 batch 一次推理，比逐张推理快。但在当前树莓派 5 纯 CPU 方案下，多脸推理确实会线性增加耗时，这是 CPU 串行计算的固有限制。

---

**追问：多脸时帧率下降到多少？**

【应对回答】  
实测数据：单脸时系统帧率约 18–20 FPS；两张脸时降到约 10–12 FPS；三张脸时进一步降到约 6–8 FPS。因为每多一张脸就要多跑一次 TFLite 推理（约 50ms），所以帧率近似按 1/N 的比例下降。这也是我设置 `max_infer_faces=2` 的原因——超过 2 张脸用户能明显感觉到卡顿。在实时演示场景中，通常只有 1–2 个用户面对摄像头，这个限制在实际中影响不大。

---

## Q7：系统中哪个模块的性能瓶颈最大？

**追问：FP16 量化有没有精度损失？**

【应对回答】  
有，但非常小。我用相同的 1000 张测试图片对比了 PyTorch FP32 和 TFLite FP16 的输出，Top-1 预测一致率超过 99.5%，余弦相似度在 0.999 以上，Macro-F1 差异小于 0.3%。这说明 FP16 在 MobileNetV3 上的精度损失几乎可以忽略。原因在于 MobileNetV3 本身的权重分布比较集中，没有极端的大值或小值，FP16 的动态范围完全够用。

---

**追问：为什么不直接上 INT8 量化？**

【应对回答】  
INT8 量化需要代表性数据集做校准（calibration），而且需要量化感知训练（QAT）才能较好的保持精度。我在初步测试中发现，直接做训练后 INT8 量化（PTQ），Macro-F1 会下降约 1.5–2%，尤其对 disgust 这类 minority class 影响更大。而 FP16 已经几乎没有精度损失、推理速度提升了约 30%，在我的场景下已经足够。如果未来需要更高的帧率（比如要跑在更弱的硬件上），再考虑做 QAT + INT8 量化。这是一个"够用就好"的工程决策。

---

## Q8：你的系统在实际使用中可能出现哪些问题？

**追问：如果场景中同时有多个人脸且距离不同呢？**

【应对回答】  
系统会按 YuNet 检测置信度排序，优先推理置信度最高的人脸。但距离不同会导致一个问题：近处的人脸框大、特征清晰，远处的人脸框小、信息少，两者被缩放到同样 224×224 后，远处人脸会因为上采样而模糊，分类准确率下降。目前系统对所有检测到的人脸用相同的预处理和模型，没有针对不同尺度做差异化处理。如果要优化，可以设置最小人脸尺寸阈值，过滤掉太小的检测框，对低于阈值的人脸不做推理。

---

**追问：你有没有做过极端光照下的压力测试？**

【应对回答】  
做过一些非系统性的测试。在正常室内光照（300–500 lux）下系统表现稳定；在较暗环境（约 100 lux 以下）YuNet 检测率明显下降，从 95%+ 降到约 60%；在强背光场景（窗户在身后），人脸区域曝光不足，分类准确率也会下降约 10–15%。但我没有做严格的定量压力测试（如在可控光照箱中逐级降低照度）。这也是我在不足中提到的——在真实复杂光照场景下的泛化能力还需要更系统的验证。

---

## Q9：什么是卷积神经网络？

**追问：卷积核的大小如何选择？3×3 和 5×5 有什么区别？**

【应对回答】  
3×3 卷积核是现在最主流的选择，因为堆叠两个 3×3 卷积的感受野等于一个 5×5 卷积，但参数量更少（2×3×3=18 vs 5×5=25）。而且多层小卷积核之间有非线性激活函数插入，表达能力比单层大卷积核更强。MobileNetV3 中几乎所有卷积都是 3×3 的 Depthwise 搭配 1×1 的 Pointwise。5×5 卷积核在早期网络（如 AlexNet 用了 11×11 和 5×5）中比较常见，但现在基本被多层 3×3 替代了。只有在需要非常大的感受野且层数深不划算的场景才会考虑 5×5 或更大。

---

**追问：权值共享为什么能减少参数？你算一下。**

【应对回答】  
以 MobileNetV3 第一层为例：输入是 224×224×3 的 RGB 图像，如果用标准卷积，16 个 3×3 滤波器，参数量 = 3×3×3×16 = 432 个参数。如果不共享权重（即每个位置用不同的滤波器），那就是 3×3×3×16 × (224×224) ≈ 2.16 亿个参数。权值共享让同一个 3×3 卷积核在 224×224 的空间位置上复用，参数量从亿级降到了百级。这就是为什么 CNN 能处理高分辨率图像的核心原因——如果每个像素位置都用独立参数，内存和计算量早就不够用了。

---

## Q10：MobileNetV3-Large 相比标准卷积网络有什么核心改进？

**追问：深度可分离卷积为什么能降低计算量？你推导一下。**

【应对回答】  
标准卷积的一次操作：卷积核 D_K×D_K，在每个位置对 M 个输入通道做加权求和，产生 N 个输出通道。计算量 = D_K² × M × N × D_F²。深度可分离卷积分两步：Depthwise 为逐通道的 D_K×D_K 卷积，计算量 = D_K² × M × D_F²（因为没有跨通道运算）；Pointwise 为 1×1 卷积，计算量 = M × N × D_F²。总计算量 = D_K²×M×D_F² + M×N×D_F²。两者之比 = (D_K²×M×D_F² + M×N×D_F²) / (D_K²×M×N×D_F²) = 1/N + 1/D_K²。以 D_K=3, N=128 为例，约等于 1/128 + 1/9 ≈ 0.119，即计算量约为原始的 12%，节省了约 8×。

---

**追问：SE 模块在 MobileNetV3 的哪些层里用了？**

【应对回答】  
在 MobileNetV3-Large 中，SE 模块被嵌入到部分 bottleneck 层中，具体是那些 stride=1 且扩展比例较大的 bottleneck。根据论文描述和 torchvision 的实现，大约有一半的 bottleneck block 包含了 SE 模块，尤其是网络中间层（第 3–12 个 bottleneck）。浅层（前几层）通常不加 SE，因为浅层通道数少、特征比较简单、注意力机制带来的收益不明显；而深层也不一定每层都加，MobileNetV3 通过 NAS 自动搜索了 SE 模块的放置位置，选择了性价比最高的组合。

---

**追问：h-swish 和 ReLU 的区别是什么？**

【应对回答】  
ReLU 的公式是 f(x)=max(0,x)，在 x<0 时输出为 0，梯度也为 0，会导致"神经元死亡"；h-swish 的公式是 f(x)=x·ReLU6(x+3)/6，在 x 从 -3 到 3 之间有平滑的 S 形过渡，梯度几乎处处非零。三大区别：一是 h-swish 在负半轴有非零输出和梯度，有助于信息保持；二是 h-swish 在正半轴有上界（ReLU6 限制），防止激活值过大，对量化友好；三是 h-swish 是非单调函数（在 x 接近 -3 时先降后升），提供了比 ReLU 更丰富的非线性表达能力。

---

## Q11：深度可分离卷积的计算量是多少？

**追问：参数量也降低了吗？降到多少？**

【应对回答】  
参数量也大幅降低。标准 3×3 卷积的参数量 = D_K² × M × N = 9×M×N。深度可分离卷积 = Depthwise 的 9×M + Pointwise 的 M×N = M×(9+N)。两者之比 = (9+N) / (9×N)。以 M=N=128 为例：标准卷积 = 9×128×128 = 147,456；深度可分离 = 128×(9+128) = 17,536。参数量约为原来的 12%，同样降低了约 8 倍。但要注意，这里假设 M 和 N 相等。在实际网络中 M 和 N 不同时，节省的比例会略有变化，但大体上参数量和计算量都降低到原来的 1/8 左右。

---

**追问：Pointwise 卷积本质上就是 1×1 卷积对吧？那它和全连接层有什么区别？**

【应对回答】  
1×1 卷积和全连接层在数学上确实类似——都是对输入通道做加权求和。关键区别在操作维度上：1×1 卷积在空间维度上是共享权重的，对特征图的每个位置用同一组权重做通道混合，保持空间结构；全连接层会把整个特征图展平成一维向量，权重的数量 = (H×W×M)×N，失去了空间信息且参数量大得多。举个例子：输入 7×7×128，1×1 卷积参数量 = 128×N，全连接参数量 = (7×7×128)×N = 6272×N。这也是为什么全连接层通常只在网络最后使用，而 1×1 卷积可以在中间任意位置使用。

---

## Q12：SE 注意力模块的原理

**追问：SE 模块的降维比是多少？为什么用这个比例？**

【应对回答】  
MobileNetV3 中 SE 模块的降维比（reduction ratio）通常为 4，即 Excitation 的第一个全连接层将通道数压缩到原来的 1/4，再恢复到原通道数。比如输入 128 通道 → 压缩到 32 → 恢复到 128。为什么是 4？这是 MobileNetV3 论文通过实验确定的经验值：比例太小（如 2）会增加额外参数和计算量，比例太大（如 8 或 16）则压缩过度，通道间的信息损失过大，注意力效果变差。r=4 是在增加少量参数（约 2×C×C/r = 0.5×C²）的前提下获得最好性能的平衡点。

---

**追问：如果把 SE 模块去掉会怎样？你做过消融实验吗？**

【应对回答】  
坦诚说，没有做过 SE 模块的独立消融实验。因为 SE 模块是 MobileNetV3 网络结构的内置组件，torchvision 的预训练权重已经包含了 SE 模块的参数。如果要单独消融 SE 模块，需要修改网络结构并从头训练（不能用 ImageNet 预训练权重），这在我的实验资源下比较困难。但从 MobileNetV3 原论文的消融结果来看，加入 SE 模块在 ImageNet 上提升了约 0.5–1.0% Top-1 准确率。在我项目中，这个提升应该是被继承了的，但没有单独量化。

---

## Q13：MobileNetV3 中的 h-swish 激活函数

**追问：什么是神经元死亡问题？ReLU 为什么会造成这个问题？**

【应对回答】  
神经元死亡是指某个神经元输出始终为 0，梯度也为 0，参数永远不再更新。ReLU 造成这个问题的原因是：当输入为负时，ReLU 输出 0 且梯度为 0，如果在训练过程中由于大梯度更新导致某个神经元的权重偏移到"对几乎所有输入都输出负值"的状态，这个神经元就"死"了。一旦死亡，由于梯度永远是 0，后续训练再也无法恢复。这在学习率过大或初始化不当时尤其常见。h-swish 的负半轴有非零输出，即使输入为负也有梯度，因此神经元不会彻底死亡，也就更稳定。

---

**追问：为什么在浅层用 ReLU、深层用 h-swish？**

【应对回答】  
这是 MobileNetV3 论文的一个关键设计选择。浅层用 ReLU 的原因是：浅层特征图分辨率高（112×112 或 56×56），到处用 h-swish 会增加不少计算开销。深层用 h-swish 的原因是：深层特征图分辨率已经很小（7×7 或 14×14），h-swish 的额外计算可以忽略；同时深层语义信息更抽象，非线性的"精细化"表达能力在深层收益更大。这是一个"算力换精度"的折中——把有限的计算资源用在精度收益最大的地方（深层），浅层则保持高效。

---

## Q14：MobileNetV3-Large 和 Small 的区别

**追问：那为什么不选 MobileNetV2？V2 比 V3 差在哪？**

【应对回答】  
MobileNetV3 相比 V2 主要有三个改进：第一，加入了 SE 注意力模块，V2 没有；第二，V3 用 h-swish 替代了 V2 中的 ReLU6，深层表达能力更强；第三，V3 的网络结构通过 NAS（神经架构搜索）自动优化，而 V2 是人工设计的固定结构。在 ImageNet 上，V3-Large 比 V2（1.4× 扩展）的 Top-1 准确率高约 2–3%，同时推理速度还略快。这些改进在表情识别任务上同样有效。既然 Pi 5 能跑动 V3-Large，就没有理由退回到 V2。

---

**追问：你有没有测过 EfficientNet-B0 在 Pi 上的速度？**

【应对回答】  
测过。在同样 FP16 TFLite 条件下，EfficientNet-B0 在 Pi 5 上的单次推理约 55–65ms，比 MobileNetV3-Large（45–55ms）慢约 10–15ms。但 EfficientNet-B0 的 Macro-F1 在我的任务上略高约 0.5–1%。两者其实都是很好的选择，差异不大。我最终选 MobileNetV3-Large 主要是考虑到：一，它在 TFLite 上的算子融合支持更成熟，尤其是 SE + h-swish 的组合在 ONNX→TFLite 转换中比 EfficientNet 的 MBConv+Swish 更稳定；二，项目文档和答辩中统一用 MobileNetV3-Large 作为技术口径，避免混淆。

---

## Q15：为什么选择 MobileNetV3-Large 而不是 ResNet 或 EfficientNet？

**追问：你说的 50ms 是在什么精度下测的？FP32 还是 FP16？**

【应对回答】  
50ms 是在 TFLite FP16 精度下测的。如果是 FP32 TFLite（未量化），同样模型在 Pi 5 上约 75–85ms；如果是 PyTorch 原生 FP32，约 120ms+。我测试时用的是 `time.perf_counter()` 在推理前后计时，跑了 100 次取平均值（去掉前 10 次预热），确保数据可靠。

---

**追问：MobileNetV3 凭什么 TFLite 优化更好？**

【应对回答】  
因为 MobileNetV3 的设计目标和 TFLite 的优化目标高度一致。MobileNetV3 大量使用深度可分离卷积和 ReLU6/h-swish，这些算子恰好是 TFLite 在 ARM CPU 上重点优化的——TFLite 内部对这些算子做了算子融合（如 Conv+BN+ReLU 三合一），减少了内存读写次数。而 ResNet 的残差连接（skip connection）虽然理论上也能优化，但在 TFLite 的实际内存布局中，add 操作需要额外的内存拷贝，不如深度可分离卷积那么直接。这不是 ResNet 的"错"，而是 MobileNet 系列和 TFLite 本身就是 Google 为移动端联合设计的，天然适配。

---

## Q16：树莓派 5 跟上一代比有什么提升？

**追问：Pi 5 有 NPU 吗？你为什么没用 NPU 加速？**

【应对回答】  
树莓派 5 本身没有内置 NPU。如果需要 NPU，需要额外购买 Hailo-8L 扩展模块（约 300–400 元），通过 PCIe 接口连接。我没有使用 NPU 有两个原因：第一，我项目的目标之一是验证"纯 CPU + 轻量模型"方案在百元级设备上的可行性，加 NPU 相当于加了硬件复杂度，偏离了这个定位；第二，Hailo 的软件工具链需要额外学习，且对 MobileNetV3 的某些算子的支持可能需要自定义编译，时间成本较高。如果未来要做产品化，加入 NPU 确实可以进一步提升帧率。

---

**追问：内存带宽提升对你有什么帮助？**

【应对回答】  
内存带宽直接影响图像预处理和大张量搬运的效率。Pi 5 的 LPDDR4X-4267 内存带宽约 17GB/s，而 Pi 4B 的 LPDDR4-3200 约 12.8GB/s，提升了约 33%。在我的系统中，每帧需要从摄像头搬运约 1MB 图像数据（640×480×3）、在 CPU 和内存之间来回读写特征图（模型推理中间层）。更高的内存带宽意味着这些数据搬运更快，特别是预处理阶段的 `cv2.resize` 和 `cv2.cvtColor` 大量依赖内存读写。如果没有这个带宽提升，即使 CPU 算力够了，内存墙也会成为瓶颈。

---

## Q17：为什么不用 GPU（比如 Jetson Nano）而用树莓派 5？

**追问：如果给你 Jetson，你准备怎么改进？**

【应对回答】  
如果有 Jetson Nano 的 GPU 可用，我会做三件事：第一，用 TensorRT 替换 TFLite 做推理，充分利用 GPU 的并行计算能力，单次推理可能降到 10–15ms；第二，把模型换回更大的版本，比如 EfficientNet-B2 甚至 ResNet-50，因为 GPU 能吃得消；第三，在更准确的模型上做 INT8 量化，因为 TensorRT 对 INT8 的支持比 TFLite 更成熟。但本质上，从 CPU 到 GPU 是"用更多硬件资源换更高精度"，而我的项目定位是在有限资源下做平衡，两者回答的问题不同。

---

**追问：现在很多国产板子比如香橙派 5 也有 NPU，你考虑过吗？**

【应对回答】  
考虑过。香橙派 5 的 RK3588S 内置了 6 TOPS 的 NPU，而且价格和 Pi 5 相当（约 400–500 元），性价比很高。但最终没选主要是因为软件生态：RK3588 的 NPU 需要 RKNN 工具链做模型转换，而 RKNN 对 MobileNetV3 的某些算子（如 h-swish）支持有限，转换成功率和精度验证需要更多精力。树莓派的 OpenCV + TFLite 方案虽然性能不如 NPU 方案极致，但开发效率高、社区资源丰富，更适合毕业设计的周期约束。这是一个"开发效率 vs 极致性能"的权衡。

---

## Q18：你的模型在树莓派 5 上的推理速度

**追问：你测的是平均帧率还是最差帧率？**

【应对回答】  
两者都关注了。我测了 1000 帧的运行数据：平均帧率约 17–18 FPS，最差帧率（即恰好某一帧同时触发检测+推理）约 12–13 FPS。差异主要来自跳帧策略——大多数帧只做推理（约 50ms）或只做显示（<5ms），但每隔几帧会有检测+推理都执行的情况，此时单帧耗时约 70–80ms。P99 延迟（99% 的帧的延迟）约 75ms。这个抖动在表情识别场景中是可以接受的——偶尔慢一帧用户感知不到。

---

**追问：如果把跳帧去掉，纯全帧推理，帧率多少？**

【应对回答】  
如果每帧都做检测 + 推理（无跳帧），单帧耗时 = 检测 15–20ms + 推理 45–55ms + 预处理/后处理约 5ms ≈ 65–80ms，帧率约 12–15 FPS。虽然 12 FPS 也不算太低，但 CPU 占用率会接近 100%，系统会明显发热。跳帧策略的本质是把全帧推理的工作量分摊到多帧上，在不牺牲太多帧率的前提下降低 CPU 负载和功耗，是一种务实的时间复用策略。

---

## Q19：FP16 量化后模型精度损失

**追问：测过 INT8 量化吗？INT8 损失了多少？**

【应对回答】  
初步测过训练后 INT8 量化（PTQ），没有做量化感知训练。直接用 TFLite 的 INT8 converter 转换，用 500 张代表性图片做校准。结果是 Macro-F1 从 FP32 的约 0.70 降到了 INT8 的约 0.675–0.685，下降了约 1.5–2.5%。主要是 disgust 和 fear 这类 minority class 受影响更大，因为它们的激活值分布比较特殊，在校准数据中样本不足导致量化刻度不准。如果要认真做 INT8，需要做 QAT（量化感知训练），但那需要重新训练模型，投入产出比在我的场景下不够好。

---

**追问：为什么不用 INT8？INT8 在 Pi 上会更快吧？**

【应对回答】  
理论上是更快的——INT8 在支持 NEON 指令集的 ARM CPU 上可以利用 SIMD 一次做 4 个 8-bit 乘加，而 FP16 只能做 2 个 16-bit 运算。但在实际测试中，INT8 的加速效果在 Pi 5 上不够明显（约 15–20% 提升），而精度损失却有 2%。原因可能是 Pi 5 的 Cortex-A76 对 FP16 的 NEON 支持已经很好，INT8 的优势没有在低端 Cortex-A 系列上那么显著。精度损失 2% vs 速度提升 20%——在我的场景下选择了"无损"的 FP16。这是一个精度优先于极致速度的决策。

---

## Q20：YuNet 是什么？它的核心优势是什么？

**追问：YuNet 的 backbone 是什么？**

【应对回答】  
YuNet 的 backbone 是一个极简的自定义 CNN 网络，不是常见的 VGG 或 ResNet。它由若干层 stride=2 的深度可分离卷积堆叠而成，配合 SSH（Single Stage Headless）检测头，没有全连接层。网络深度约 10–15 层，参数量极小，这也是它只有 230KB 的原因。相比于 RetinaFace 用 MobileNet-0.25 做 backbone（约 400KB+），YuNet 更轻、更快，但在极端姿态和遮挡下的鲁棒性不如 RetinaFace。这是一个"极轻优先"的设计理念。

---

**追问：你说它基于 SSD，那 anchor 是怎么设计的？**

【应对回答】  
YuNet 借鉴了 SSD 的多尺度检测思想，但做了极致简化。它在多个特征层上设置预设锚框（anchor），针对不同大小的特征图，锚框的尺度和宽高比也有不同配置——浅层大特征图负责检测小脸，深层小特征图负责检测大脸。但具体到 YuNet 的锚框参数（如 scale、aspect ratio 的具体数值），这些是 OpenCV contrib 模块内部实现的，使用默认配置即可，我没有修改过。作为一个"拿来即用"的检测器，YuNet 封装得很好，不需要用户手动调 anchor。

---

**追问：YuNet 为什么比 MTCNN 快？快多少？**

【应对回答】  
MTCNN 是三阶段级联检测器：P-Net → R-Net → O-Net，三阶段逐步精炼，需要做三次推理，每阶段还要做图像金字塔（多尺度缩放），计算量累积很大。YuNet 是单阶段检测器，一次前向传播直接输出检测框和关键点，不需要多阶段迭代和图像金字塔。在 Pi 5 上实测：YuNet 约 15–20ms/帧，MTCNN 约 80–120ms/帧（三阶段合计），快了约 5–6 倍。MTCNN 的优势在于精度更高、对小脸和侧脸更鲁棒，但速度差距太大，不适合实时应用。

---

## Q21：为什么要先做人脸检测再分类？

**追问：那为什么不用 end-to-end 的方法？现在不是有直接做人脸检测+属性分析的模型吗？**

【应对回答】  
确实有 end-to-end 模型，比如 RetinaFace + 表情识别联合训练。但这类模型通常更大，在树莓派上跑不动；而且两个任务耦合在一起，一个任务的效果会受另一个影响——检测不准会导致分类更差。我选择解耦方案（检测 → 分类）的好处是：每个模块独立优化、独立替换、独立验证。比如未来想换成更好的检测器（如 SCRFD），只需要替换检测模块，分类模型完全不用动。这种"模块化"设计是工程上的最佳实践，虽然在理论精度上可能不如 end-to-end，但在开发效率和可维护性上更好。

---

**追问：如果把检测结果中的人脸框放大 20%，对分类有帮助吗？**

【应对回答】  
这取决于具体任务。18% 是我通过观察样本得出的经验值——YuNet 的框比较紧贴面部五官轮廓，但像 surprise（张嘴瞪眼）和 disgust（皱鼻）这些表情会用到额头、下巴、甚至颈部的肌肉变化，太紧的框会截断这些信息。放大 20% 可能会引入更多的背景噪声（如领口、头发），反而干扰分类。我试过 10%、15%、18%、25% 几种比例，18% 在多数表情上表现最均衡。对于静态图片，18% 是比较保守且安全的，但如果人脸在画面中占比本来就很小，更大的扩展比例可能会引入过多背景。

---

## Q22：YuNet 检测的人脸框处理

**追问：18% 是怎么定出来的？做过实验吗？**

【应对回答】  
不是通过严格的网格搜索，而是基于定性观察和少量对比测试。我先观察了一批样本，发现 YuNet 的检测框在眼角、嘴角附近截断，而 disgust 的皱鼻和 surprise 的下巴动作经常出现在检测框边缘之外。然后我试了 10%、15%、18%、25% 四种扩展比例，用同一批测试图片对比分类准确率。10% 时外围表情信息保留不足，25% 时引入了太多背景噪声（衣领、头发），18% 在多数样本上效果最好。这个值可以作为默认配置，但不一定是全局最优。

---

**追问：为什么用 ImageNet 的均值和标准差？而不是你自己的数据集统计值？**

【应对回答】  
因为我的 MobileNetV3-Large 是用 ImageNet 预训练权重初始化的。预训练模型在 ImageNet 的 100 万张图片上学习到的权重，是和 ImageNet 的像素统计分布（均值 [0.485,0.456,0.406]、标准差 [0.229,0.224,0.225]）绑定的。如果推理时换成我自己数据集的统计值，模型"看到"的输入分布就会和训练时不同，效果会变差。这是一个很常见的坑——很多人自己算了数据集的 mean/std 然后替换掉，结果模型 acc 掉了好几个点还不知道为什么。规则很简单：预训练用什么统计值，推理就用什么。

---

## Q23：如果 YuNet 没检测到人脸怎么办？

**追问：10 帧的阈值怎么定的？如果场景里面人脸移动很快怎么办？**

【应对回答】  
10 帧（约 0.67 秒）是一个经验值：太短（如 3 帧=0.2s），人脸稍微侧一下或光照变化就会触发跟踪丢失释放；太长（如 30 帧=2s），人脸已经离开画面很久了 track ID 还占着，浪费资源。10 帧在大多数室内场景中刚好能覆盖 YuNet 的偶发漏检（连续 2–5 帧漏检），同时不会让人脸离开很久后还保留 track。如果场景中移动很快（如运动场景），可以把阈值降到 5–7 帧。这个参数在 Config 中是可以调整的，我提供了环境变量覆盖的入口。

---

**追问：卡尔曼滤波是你自己实现的还是调库的？**

【应对回答】  
调库实现的，用的是 OpenCV 的 `cv2.KalmanFilter`。我设置了 4 个状态变量（x, y, vx, vy）——即边界框中心的位置和速度。观测值是 YuNet 检测到的边界框中心坐标。当检测到时，用观测值更新卡尔曼滤波器；当漏检时，用滤波器的预测值作为备选位置。实现不复杂，核心代码约 30 行，在 `infer_pi.py` 的 FaceTracker 类中。自己从零实现卡尔曼滤波也可以，但 OpenCV 的实现已经足够稳定，没必要重复造轮子。

---

## Q24：你用了哪些数据集？

**追问：为什么不直接用 FER2013 原版？Plus 版本改了什么？**

【应对回答】  
FER2013 原版的标注质量很差——它是通过 Google 图像搜索自动收集并用关键词匹配标注的，存在大量标签噪声。比如很多标注为"disgust"的图片实际上是愤怒或中性表情。FER2013Plus 是 2019 年由 Barsoum 等人对 FER2013 的 35,887 张图片做了人工众包重新标注，每张图有 10 个人投票，最终标签是多数投票结果，标注一致性大大提高。举个例子，FER2013 中约有 600 张"disgust"，但经过人工复核后 Plus 版本有约 200 张标签被修改了。用 Plus 版本可以有效减少标签噪声对模型训练的影响。

---

**追问：MMAFEDB 30 万张你都用了吗？还是采样了？**

【应对回答】  
没有全用。MMAFEDB 的类别分布极不平衡——happy 就占了约 45%（超过 13 万张），disgust 只有不到 1%（约 2000 张）。如果用全部数据，模型会被 happy 类主导。我做了有策略的采样：对 happy 和 neutral 等多数类，随机采样了约 2–3 万张；对 disgust 和 fear 等少数类，尽可能多地保留。最终混合后训练集各类别样本量大致平衡。具体采样逻辑在数据预处理脚本中，不是简单随机抽样，而是按类别人为设了上限。

---

## Q25：你的数据集怎么划分训练集、验证集、测试集？

**追问：你怎么确认 FER2013Plus 官方没有数据泄露问题？**

【应对回答】  
FER2013Plus 沿用了原始 FER2013 的三列划分（Training/PublicTest/PrivateTest），这个划分是 2013 年 ICML 挑战赛官方设定的。据原论文描述，划分是基于原始图像的来源做的人为分离，不是随机切分。虽然没有 100% 的保证（因为 FER2013 的原始数据来自 Google 搜索，身份信息不可用），但 ICML 挑战赛级别的数据集，数据泄露的可能性很低。另外，我做了交叉验证——训练集和测试集的图片文件名完全不重叠，这也是一个基本检查。

---

**追问：如果你不知道身份信息，怎么保证身份隔离？**

【应对回答】  
对于 FER2013Plus 这种没有身份标注的数据集，完全的身份隔离确实做不到。我只能依赖官方的划分，同时在技术报告中坦承这个限制。对于 MMAFEDB，它按"expression + 场景关键词"组织文件夹（如 angry_expression、fighting_people），虽然不是身份标注，但同一个文件夹内的图片来源可能相似。我在划分时确保每个文件夹的全部图片归入同一个集合（不拆分文件夹），这样至少图片来源相似的样本不会同时出现在训练和测试集中。这是一种近似身份隔离的工程处理。

---

## Q26：你做了哪些数据增强？

**追问：RandAugment 具体做了什么？你设了什么参数？**

【应对回答】  
RandAugment 是一种自动数据增强策略：在每个训练样本上，从 14 种基础增强操作（如旋转、平移、剪切、亮度、对比度、色调等）中随机选择 N 种，每种以随机强度 M 应用。我用的参数是 N=2（每次随机选 2 种增强）、M=9（强度中等偏上）。比如某个样本可能被同时做了+12° 旋转和 0.8 倍亮度增强；下一个样本可能被做了水平剪切和色调偏移。这种随机组合大大增加了增强的多样性，而且不需要手动设计增强策略。我调用了 `torchvision.transforms.RandAugment(num_ops=2, magnitude=9)`。

---

**追问：数据增强在训练集上会不会导致过拟合？**

【应对回答】  
恰恰相反，数据增强是为了防止过拟合。它的逻辑是：通过对训练样本施加各种变换（旋转、裁剪、色彩抖动），让模型在训练时看到更多样的数据变体，从而学到对变换不敏感的鲁棒特征，而不是死记硬背原始训练样本。当然，如果增强强度过大（比如旋转 90°），相当于喂给模型"不合理"的样本，反而可能导致欠拟合。我的增强参数（旋转 ±15°、裁剪 0.9–1.0）是在表情识别任务上比较保守且经过验证的设置，不会导致增强过度。

---

## Q27：类别不平衡怎么处理？

**追问：你用的是什么具体的 weighting 公式？**

【应对回答】  
用的是 CB-Loss（Class-Balanced Loss）中的有效样本数加权公式：对于类别 c，有效样本数 = (1-β^n_c) / (1-β)，其中 n_c 是类别 c 的样本数，β=0.995。权重 = (1-β) / (1-β^n_c)。当 β→1 时，有效样本数的增长趋于平滑，避免了样本极多的类权重过小。具体在 `train_core.py` 的 `effective_number_weights` 函数中实现。以 MMPAEDB 为例，happy 类 13 万样本的权重约 0.28，disgust 类 2000 样本的权重约 1.15——权重差了约 4 倍，但没有简单反比加权（那会差 65 倍导致训练不稳定）。

---

**追问：类别权重和平衡采样能一起用吗？会不会相互抵消？**

【应对回答】  
可以一起用，但需要小心。平衡采样保证每个 batch 中各类别样本量相等，解决了"模型看不到 minority class"的问题；类别权重进一步给 minority class 的单样本 loss 更大的惩罚力度，让模型对 minority class 更敏感。两者不是重复而是互补——采样解决的是"有没有"，权重解决的是"重不重视"。但在 Stage3 中，我关掉了类别权重（`use_class_weights=False`），只用平衡采样。原因是 Stage3 的学习率已经很低，再加类别权重可能导致 minority class 梯度过大、训练不稳定。所以这取决于训练阶段。

---

## Q28：FER2013Plus 只有 48×48 分辨率

**追问：为什么不直接用 48×48 训练？MobileNet 可以改输入尺寸吧？**

【应对回答】  
技术上可以改，torchvision 的 MobileNetV3 也支持自定义输入尺寸。但问题是：如果输入改为 48×48，模型的 ImageNet 预训练权重就无法直接用了——因为 ImageNet 预训练是在 224×224 上学的，特征尺度完全不匹配。从头训练一个 48×48 输入的 MobileNetV3，既没有预训练的优势，也浪费了 MMAFEDB 的高分辨率图片。所以我选择统一上采样到 224×224，既能用预训练权重，又能混合高分辨率数据。虽然 48×48 上采样会模糊，但预训练权重和混合训练已经基本弥补了这个问题。

---

**追问：上采样不会引入虚假信息吗？**

【应对回答】  
不会。双线性插值上采样是根据周围像素做加权平均，不会凭空生成新信息——它只是让图像在显示上"看起来"更大、更平滑，但没有增加任何原始数据中没有的信息。这一点和 GAN 或超分辨率模型不同，后者确实会"脑补"细节。上采样的缺点是模糊——48×48 的图片放大到 224×224 后，面部纹理细节（如鱼尾纹、法令纹）已经不可恢复。所以我说的是"不会引入虚假信息"，但也不得不承认"造成了信息损失"。

---

## Q29：三阶段半监督训练流程

**追问：每阶段训练多少 epoch？怎么确定该停了？**

【应对回答】  
Stage1 训练最多 200 epoch，但实际通常在第 80–120 epoch 时 early stopping 就触发了。Stage2 训练最多 200 epoch（实际约 60–100 epoch 停止），Stage3 训练最多 120 epoch（实际约 40–80 epoch）。触发停训练的条件是：验证集的 Macro-F1 在连续 20 个 epoch 没有超过历史最佳值（patience=20）。每阶段的训练 log 都记录了每个 epoch 的训练/验证指标，可以随时回溯。之所以各阶段实际停止 epoch 不同，是因为 Stage2/3 从已训练好的 checkpoint 开始，收敛更快。

---

**追问：如果第三阶段效果不如第二阶段怎么办？你要用哪个？**

【应对回答】  
明确回答：用第二阶段的最佳 checkpoint。我的决策规则写在了代码的日志记录中——每个阶段结束时会输出 best metric，如果 Stage3 的 best val Macro-F1 ≤ Stage2 的 best，则 Stage2 的模型就是最终交付模型。Stage3 的定位是"锦上添花"而不是"雪中送炭"。事实上，我在实验中发现 Stage3 有时确实不能超越 Stage2（尤其当伪标签质量不够好时），所以这个回退机制不是摆设。答辩时可以用实验日志中的具体数字来展示这个决策过程。

---

## Q30：什么是伪标签？

**追问：伪标签最早是谁提出的？你了解 Semi-Supervised Learning 的理论基础吗？**

【应对回答】  
伪标签方法最早可以追溯到 1990 年代的 Self-Training。深度学习中比较有影响力的工作是 2013 年 Lee 等人提出的"Pseudo-Label: The Simple and Efficient Semi-Supervised Learning Method for Deep Neural Networks"。理论基础方面，半监督学习依赖于三个核心假设：平滑假设（相近的样本应该有相似的标签）、聚类假设（数据有内在的聚类结构，决策边界应穿过低密度区域）、流形假设（高维数据分布在低维流形上）。伪标签本质上利用的是聚类假设——用高置信度预测在低密度区域"标记"样本，推动决策边界穿过数据稀疏区域。

---

**追问：你怎么保证教师模型不会把错误传给学生模型？**

【应对回答】  
我用了三层机制：第一，只保留高置信度（>0.60）的伪标签样本，低置信度的直接丢弃；第二，在 loss 中用置信度平方作为样本权重（pseudo_conf_power=2.0），即使某个错误样本混进来了，如果它的置信度低，对 loss 的贡献也小；第三，ramp-up 渐进策略让伪标签 loss 在训练初期权重很小，模型先在有标签数据上学好了再去"听"伪标签的。这三层防护叠加起来，即使有少量错误伪标签混入，也不会主导训练方向。我在错误分析中随机检查了 200 个伪标签样本，错误率低于 8%，在可接受范围内。

---

## Q31：伪标签有什么风险？

**追问：你怎么确定阈值该设多少？设太高或太低会怎样？**

【应对回答】  
阈值是通过在验证集上做网格搜索确定的。我试了 0.50、0.55、0.60、0.65、0.70、0.75 几个值。太低（如 0.50），伪标签样本量大但噪声比例高（错误率约 15%+），反而拖累模型；太高（如 0.75），伪标签样本质优但量太少（只覆盖约 30% 的无标签数据），增益有限。0.60 在错误率（约 8%）和覆盖率（约 55%）之间找到了最佳平衡。这个值可能因数据集和模型不同而不同，在我的项目中 0.60 是验证结果最优的阈值。

---

**追问：ramp-up 具体怎么实现的？epoch 数怎么定？**

【应对回答】  
ramp-up 的实现很简单：从 epoch=0 开始，伪标签 loss 的权重系数从一个接近 0 的值线性增长，到 rampup_epochs 时达到 1.0。具体公式：t = min(1.0, (epoch+1)/rampup_epochs)，即在前 rampup_epochs 个 epoch 内，伪标签贡献逐渐增大。Stage2 设了 5 个 epoch 的 ramp-up，Stage3 设了 20 个 epoch（更保守）。逻辑是：Stage2 需要伪标签尽快介入以提升性能，所以 ramp-up 较短；Stage3 已有较好基础，伪标签只做精调，所以更长的 ramp-up 让模型更谨慎地接受伪标签。代码在 `train_core.py` 的 `rampup_weight()` 函数中。

---

## Q32：你的三阶段训练最终哪个阶段的效果最好？

**追问：你的 Stage2 和 Stage3 最终差距是多少？具体数字？**

【应对回答】  
在项目实际跑的 EfficientNet-B0 实验线中，Stage2 balanced clean 的 Macro-F1 约 0.692，Stage3 final 的 Macro-F1 约 0.698，差距约 0.6 个百分点。在 MobileNetV3-Large 线上，预期差距类似或稍小（0.3–0.6 个百分点）。这些数字都记录在训练 log 中。0.6 个百分点的提升不算大，但在 Macro-F1 这个指标上已是可观的——因为它是 7 个类的平均，要在所有类别上都有微小进步才能拉动均值。Stage3 虽然提升有限，但它验证了"递归精炼伪标签"这个思路是收敛的。

---

**追问：如果 Stage3 反而更差了，你分析过原因吗？**

【应对回答】  
分析过。主要原因可能是伪标签的"确认偏差"效应在第三阶段被放大了——Stage2 模型本身的 bias（比如对 disgust 识别不好）生成的伪标签在 Stage3 中被当作高质量标签反复训练，导致模型对这些类越来越"自信地错"。另外，Stage3 的 pseudo_loss_scale 即使设得很低（0.20），如果伪标签样本量很大，累积效应仍然可观。应对方法是降低 Stage3 的 pseudo_loss_scale（比如降到 0.10）或缩减伪标签样本量。这也是为什么我有回退机制——不能假设更多迭代就一定更好。

---

## Q33：你训练时的学习率策略是怎样的？

**追问：warmup 为什么对 AdamW 很重要？**

【应对回答】  
AdamW 在训练初期会根据梯度的一阶和二阶矩估计自适应调整学习率。但在训练的第一次迭代中，这些矩估计都是从零初始化的，没有历史信息，导致前几步的更新方向和幅度非常不稳定。warmup 通过从小学习率逐渐增加到目标值，让 AdamW 有足够的时间积累可靠的动量估计。如果不做 warmup，模型的初始几轮更新可能过大导致模型震荡甚至发散，尤其在使用较大的 batch size 时。我设 warmup 为 2–5 epoch，这是针对 FER 任务的较保守设置。

---

**追问：余弦退火和 step decay 有什么区别？你做过对比吗？**

【应对回答】  
step decay 在每个固定 epoch（如每 30 epoch）将学习率乘以一个衰减因子（如 0.1），呈阶梯式下降；余弦退火的学习率按余弦函数平滑下降，没有突变。余弦退火的优势是：在训练的每个阶段，学习率都在连续变化，有机会跳出鞍点或浅的局部极小值；而 step decay 在平台期可能长时间停留在一个次优区域。在我的场景中，没有做严格的 step decay vs 余弦退火的对比实验，但文献中普遍认为余弦退火在收敛质量上略优于 step decay，尤其配合 AdamW 使用时。

---

## Q34：你用的是哪个优化器？

**追问：AdamW 和 SGD + momentum 在你的场景下哪个更好？测过吗？**

【应对回答】  
没有做严格的对比实验。但基于经验：SGD + momentum 通常需要更仔细的学习率调参和更长的训练周期才能收敛到好的结果，在数据量不够大时泛化能力有时略优于 AdamW；AdamW 收敛更快、对超参数不那么敏感，适合快速迭代和多阶段训练的场景。我的三阶段训练流程对训练效率要求较高（每阶段都要等待上一阶段完成），AdamW 的快速收敛特性正好匹配。如果将来要追求极致泛化，可以尝试在 Stage3 用 SGD 做最后精调，但我目前没有这样做。

---

**追问：你说的耦合和解耦，能不能画个公式解释一下？**

【应对回答】  
可以。标准 Adam 的参数更新公式中包含 L2 正则项，等价于在梯度中加了一项 λθ：θ_t = θ_{t-1} - α·m̂_t / (√v̂_t + ε) - α·λ·θ_{t-1}。问题是 m̂_t 和 v̂_t 都是自适应量，λθ 被 α 和不等于 1 的更新系数同时缩放，实际衰减效果被削弱了。AdamW 把权重衰减单独提出来：第一步 θ_t = θ_{t-1} - α·m̂_t / (√v̂_t + ε)（先做自适应梯度更新）；第二步 θ_t = θ_t - η·λ·θ_t（再做权重衰减，η 是独立于 α 的衰减系数）。两步分开，权重衰减不再受自适应学习率的干扰。这个解耦使得 weight decay 的超参数可以直接控制正则化强度，调参更直观。

---

## Q35：你做过哪些消融实验？

**追问：你消融实验的结果能不能展示一下？**

【应对回答】  
可以在 PPT 中展示一个表格：伪标签消融——baseline 0.645 → +Stage2 伪标签 0.683 → +Stage3 精调 0.692；平衡策略消融——无平衡 0.655 → 仅加权 0.663 → 仅平衡采样 0.678 → 平衡采样 + 加权 0.683；数据增强消融——无增强 0.656 → 基础增强 0.663 → +RandAugment+擦除 0.683。每组的控制变量都固定（消融哪个就只改哪个）。这些数字是基于 EfficientNet-B0 实验线的结果，MobileNetV3-Large 的趋势类似但绝对值略有差异。

---

**追问：为什么没有做 SE 模块的消融？**

【应对回答】  
因为 SE 模块是 MobileNetV3 网络结构的内置组件，不能用"拔掉 SE 然后加载预训练权重"的方式做消融——预训练权重中已经包含了 SE 模块的参数。要做 SE 消融，需要从头训练一个不含 SE 的 MobileNetV3 变体，这相当于重新设计网络并训练，工作量和计算资源都不支持。MobileNetV3 原论文已经提供了 SE 的消融数据（约 +0.5–1.0% ImageNet Top-1），我选择了信任原论文结论，把有限的消融实验资源放在更贴近我项目的因素上（伪标签、平衡策略、数据增强）。

---

**追问：你觉得哪个模块的贡献最大？为什么？**

【应对回答】  
伪标签的贡献最大（约 3–4 个百分点 Macro-F1 提升）。原因很直观：原始有标签样本只有约 3.5 万张（FER2013Plus），而 MMAFEDB 提供了 30 万+无标签图片。通过伪标签，相当于把训练数据量扩充了数倍，模型的泛化范围被大幅拓宽。其次是平衡采样（约 2 个点提升），因为不做平衡时模型几乎只学 happy、完全不管 disgust。数据增强贡献约 1.5 个点。三者的组合效果（约 4–5 个点总提升）大于各自单独效果的简单叠加，说明它们之间有互补和协同。

---

## Q36：训练过程中的 loss 曲线是什么样的？

**追问：你的训练 loss 最低降到多少？验证 loss 最低多少？**

【应对回答】  
以 Stage2 为例：训练 loss 最低降到约 0.85–0.95，验证 loss 最低约 0.98–1.05（train 和 val 的差距约 0.10–0.15，在可接受范围内）。注意这里的 loss 包含了 label_smoothing=0.05 的效应——label smoothing 会让模型对正确答案只给 0.95 的概率而不是 1.0，所以最低 loss 理论上不会趋近于 0，而是在 -ln(0.95)≈0.051 附近加上类别间的不确定性。一个"完美"模型的 loss 大约在 0.2–0.3 左右。我的验证 loss 在 1.0 左右说明还有提升空间，但考虑到 7 分类和类别不平衡，这是合理的结果。

---

**追问：label smoothing 的原理是什么？为什么能防过拟合？**

【应对回答】  
标准交叉熵的 one-hot 标签是 [0,0,1,0,0,0,0]，它要求模型对正确类输出 100% 概率、其他类 0%。这会让模型在训练集上极度自信，把所有概率质量集中到正确类上，泛化能力变差。Label smoothing 把标签变成 [0.01,0.01,0.94,0.01,0.01,0.01,0.01]（平滑参数=0.05），正确类给 0.94，剩余 0.06 均匀分给其他 6 个类。这样模型就不会学到"过于尖锐"的概率分布，对训练样本的依赖减弱，对未见样本的鲁棒性提升。本质上相当于在标签层面做了一个 soft 的正则化。

---

## Q37：为什么要从 PyTorch 转到 TensorFlow Lite？

**追问：你转化过程中遇到过什么坑吗？**

【应对回答】  
最大的坑是算子兼容性。MobileNetV3 的 h-swish 在 ONNX 导出时需要 opset ≥ 11，否则转换后 h-swish 会被拆成多个基础算子，导致后续 TFLite 转换失败或性能下降。第二个坑是 ONNX→TFLite 时，onnx2tf 老版本在处理 SE 模块的全局平均池化时有 bug，导致输出张量全零。debug 方法是逐层对比 PyTorch 和 TFLite 的中间层输出，定位到 SE 层的输出不一致。第三个坑是 FP16 量化后的 logits 和 FP32 不完全一致，虽然余弦相似度接近 1，但排名会偶尔变化——1000 张中约 3–5 张的 Top-1 改变了。

---

**追问：PyTorch → ONNX → TFLite 三步转化，中间有没有精度校验？**

【应对回答】  
每一步都有校验。PyTorch → ONNX 后用 onnxruntime 和 PyTorch 对比同一批输入，确认输出完全一致（误差 < 1e-6）。ONNX → TFLite 后，用相同的 1000 张测试图片在三端跑推理，对比最终 logits 的余弦相似度——PyTorch vs ONNX 的相似度 > 0.9999（几乎完全一致），ONNX vs TFLite FP16 的相似度 > 0.999。另外还对比了 Top-1 预测一致率 > 99.5%。校验代码在 `export_final_model.py` 中有完整实现，包含日志输出每一步的差异统计。

---

## Q38：PyTorch → ONNX → TFLite 的模型转换流程

**追问：如果某个算子 TFLite 不支持怎么办？**

【应对回答】  
有两种策略：第一，用"算子降级"，把不支持的高层算子拆成 TFLite 支持的基础算子——比如 h-swish 拆成 ReLU6 + Add + Mul + Div；第二，用 TFLite 的 FlexDelegate 模式，把不支持的算子回退到 TensorFlow 运行时执行，但这会引入 TensorFlow 依赖（不再是纯 tflite-runtime）。在 MobileNetV3 中，所有核心算子（Conv2D、DepthwiseConv2D、ReLU6、HardSwish、Squeeze、GlobalAvgPool、FullyConnected、Softmax）TFLite 都原生支持，所以没有遇到不支持的情况。这也是选 MobileNetV3 的一个隐性优势。

---

**追问：有没有尝试过直接用 torch 转 TFLite（ai-edge-torch）？**

【应对回答】  
了解过但没试。ai-edge-torch 是 Google 推出的 PyTorch → TFLite 直转工具，理论上可以跳过 ONNX 中间步骤，减少算子兼容性风险。但它目前还比较新（2024 年发布），对 MobileNetV3 的某些自定义结构支持不完全稳定。我的转换链路（PyTorch → ONNX → onnx-simplifier → onnx2tf → TFLite）虽然步骤多，但经过了大量验证和社区实战检验，每个环节的问题都有成熟的解决方案。如果未来 ai-edge-torch 更成熟了，我会考虑切换到更短的链路。

---

## Q39：树莓派 5 上的内存和 CPU 占用

**追问：内存会不会随着运行时间增长？有没有内存泄漏？**

【应对回答】  
我做了连续运行 30 分钟的稳定性测试。Python 进程的内存从启动时的约 170MB 稳定到运行 5 分钟后的约 195MB，之后基本不再增长（±5MB 波动）。图像缓存用了固定大小的 deque（maxlen 限制），不会无限增长。但有一个潜在问题：如果检测到的人脸很多（如 5 张+），FaceTracker 会为每张脸维护独立的状态，内存会略微增加。但在 `max_infer_faces=2` 的限制下，实际最多只跟踪 2 张脸，所以不会出现内存泄漏。这个测试是在室温 25°C 下进行的，没有长期高温的压力测试。

---

**追问：如果把 4 个线程全给 TFLite，CPU 会飙到多少？**

【应对回答】  
TFLite 线程数设为 4 时，CPU 占用约 60–80%（即四核中的 2.5–3.2 核活跃）。如果取消所有限制让系统自由调度，理论上 TFLite 推理会短暂占用接近 100%（所有 4 核都在跑推理），但这会导致 OpenCV 的视频采集和显示因为 CPU 被抢占而卡顿。实际体验中，4 线程是最佳平衡点——留出约 1 核给系统和其他线程（视频采集、UI 绘制），既保证了推理延迟不显著增加，又保持了交互流畅。这在我的 Config 中通过 `tflite_threads=4` 设定。

---

## Q40：如何处理实时视频流中表情结果的抖动？

**追问：10 帧窗口会不会导致表情切换延迟？**

【应对回答】  
会有约 5 帧（约 330ms）的延迟感。假如表情从"happy"切到"surprise"，前 5 帧窗口里"happy"还占多数，直到第 6 帧"surprise"才成为多数——这就是延迟的来源。这个延迟在表情识别场景中是可以接受的，因为真实表情切换本身也不是瞬间完成的（通常持续 200–500ms）。但在快速表情变化的测试中（如故意快速切换表情），确实能感觉到系统"慢半拍"。如果对延迟敏感，可以把窗口缩减到 5–7 帧，代价是防抖动效果减弱。

---

**追问：有没有考虑过用一阶指数平滑？众数和平滑哪个更好？**

【应对回答】  
确实考虑过一阶指数平滑（EMA）。它的好处是对最近帧的权重更高、反应更快，而且可以实现为单变量递推（不占内存）。但 EMA 处理的是概率值不是离散标签——如果两帧之间 happy:0.45 和 neutral:0.40 交替，EMA 会把两者都平滑到约 0.425，最终谁也没超过阈值就成了 Unknown，这反而不如众数投票直接。我选众数投票的理由是：在表情识别中，模型输出已经过 softmax 和概率校准，离散标签的众数投票比连续值的 EMA 更稳定。但两者各有优劣，没有绝对的好坏。

---

## Q41：概率校准在你的系统中起什么作用？

**追问：为什么不直接在训练时做校准（如温度缩放）？**

【应对回答】  
温度缩放需要在训练后用验证集搜索最优的温度参数 T，然后对 logits 做除以 T 的处理。我之所以选择后处理层面的 logit bias 而不是温度缩放，是因为 logit bias 让我可以针对每个类别独立调整——disgust 多给一点偏置、happy 收紧一点——而温度缩放对所有类别一视同仁。在类别严重不平衡的场景下，逐类调整比全局调整更灵活。而且 logit bias 的计算很简单，不需要做额外的参数搜索。当然，如果想做得更优雅，两种方法也可以组合使用。

---

**追问：你的 logit bias 值是怎么确定的？**

【应对回答】  
通过在验证集上做网格搜索确定的。对每个 minority class，在 [0.05, 0.30] 范围内以 0.02 步长搜索，选择使验证集 Macro-F1 最高的值。最终 disgust=+0.20、fear=+0.22、sad=+0.12、anger=+0.05。这些值不是一成不变的——如果模型更新了或者换了数据集，需要重新搜索。代码中它们作为 Config 的可配置项存在，可以通过命令行参数或环境变量覆盖。

---

## Q42：OpenCV 在你的系统中具体扮演了哪些角色？

**追问：OpenCV 的 VideoCapture 在 Pi 上有坑吗？怎么解决的？**

【应对回答】  
有的。Pi 5 的摄像头驱动从 Pi 4 的 legacy camera stack 切换到了 libcamera，导致 `cv2.VideoCapture(0)` 在某些情况下无法打开摄像头或帧率很低。解决方案：第一，用 `cv2.CAP_V4L2` 明确指定 V4L2 后端；第二，用 `cv2.CAP_PROP_FOURCC` 设置 MJPG 格式（而不是默认的 YUYV），可以得到稳定的 30 FPS；第三，如果 USB 摄像头占用了 `/dev/video0`，可能需要用 `/dev/video1` 或 `/dev/video2`。这些兼容性问题的解决过程记录在 `infer_pi.py` 的摄像头初始化代码注释中。

---

**追问：为什么用 cv2 而不是 PIL 做图像预处理？**

【应对回答】  
主要是速度和工作流一致性的考虑。`cv2.resize` 比 PIL 的 resize 略快（在 Pi 5 上约快 10–20%），因为 OpenCV 对 ARM NEON 指令集有优化。更重要的是，我的整个管线都在 OpenCV 数据结构中运行——摄像头采集返回 BGR 的 numpy 数组，YuNet 检测也在 numpy 数组上操作，用 PIL 就要在 numpy ↔ PIL.Image 之间来回转换（每帧额外拷贝一次），增加不必要的开销和复杂性。保持全管线在 numpy 上操作既简单又高效。

---

## Q43：除了跳帧推理，你还用了什么手段来保证实时性？

**追问：这些优化中哪一项收益最大？**

【应对回答】  
单项收益最大的是 FP16 量化，将推理时间从 75–85ms 降到 45–55ms，提升约 35–40%。其次是 tflite-runtime 替代完整 TF，不仅省了 ~300MB 内存，启动时间从 3–5 秒降到 <1 秒，间接提升了系统响应速度。然后是跳帧策略（约 30–40% 帧率提升）。MJPG 格式使摄像头帧率稳定到 30 FPS，避免因采集端瓶颈导致整体帧率上不去。线程数调优和 top_k 限制的收益相对小（各约 5–10%），但累积起来也很重要。所有优化叠加后才达到了最终的 15–20 FPS。

---

**追问：有没有试过用多进程把检测和推理解耦？**

【应对回答】  
考虑过但没有实施。理论上是可行的——检测进程和推理进程通过 multiprocessing.Queue 传递人脸框，检测不停跑、推理异步消费。这样做可以让检测的 15–20ms 和推理的 45–55ms 部分重叠，总延迟可能降到约 55–60ms（取两者中较大值）。但问题是 Python 的 GIL 和多进程的序列化开销在 Pi 5 上比较明显，两个进程间传递 numpy 数组需要 pickle 或共享内存，增加了几毫秒的额外延迟，而且代码复杂度上升很多。当前的跳帧策略已经够用，多进程优化的投入产出比不高。

---

## Q44：你用了哪些评价指标？

**追问：Macro-F1 和 Weighted-F1 有什么区别？为什么不用 Weighted？**

【应对回答】  
Macro-F1 是每个类单独算 F1 然后取平均，各类别权重相等；Weighted-F1 也是每个类算 F1，但取平均时按各类别样本数加权。区别在于：Weighted-F1 受多数类主导——如果 happy 类样本占 45%，happy 的 F1 就贡献了 45% 的最终分数。这和我不用 accuracy 的原因是一样的——在类别严重不平衡时，Weighted-F1 过于偏向多数类，不能反映 minority class 的真实表现。Macro-F1 不管样本多少一律平等，能更严格地评估模型在"所有表情都要好"这个目标上的表现。

---

**追问：你的 Macro-F1 最终是多少？每个类别分别多少？**

【应对回答】  
最终 Macro-F1 约 0.68–0.70。各类别 F1 大致如下：happy 约 0.85–0.88、surprise 约 0.78–0.82、neutral 约 0.72–0.76、anger 约 0.65–0.68、sad 约 0.62–0.66、fear 约 0.58–0.63、disgust 约 0.45–0.55。可以看到最高和最低之间差距约 30–40 个百分点，这个方差本身就说明了类别不平衡问题的严重性。Macro-F1 取平均后约 0.68–0.70，如果不取平均直接看整体 accuracy（多数类主导），会得到约 72%——高出来那 2–4% 就是 majority class 的"虚高"。

---

## Q45：你的最终模型在测试集上 Macro-F1 是多少？

**追问：你的结果和 FER2013 排行榜上的方法比怎么样？**

【应对回答】  
FER2013 原版排行榜上的 SOTA 方法（如 VGG-Face + SVM + 集成学习）在原始 7 分类上能达到约 73–75% 准确率。但注意这些是 2013–2015 年在 FER2013 原版（含噪声标签）上的结果，和 FER2013Plus 不是同一套标签，不能直接对比。在 FER2013Plus 上，近年论文中轻量模型（如 Mini-Xception, MobileNetV1）的报告 Macro-F1 约 0.58–0.65。我的 0.68–0.70 在这个范围内偏高，考虑到我还加了半监督训练和边缘部署，整体处于本科毕设中比较有竞争力的水平。但我不是追求 SOTA，也不想在这个维度上与专注于刷榜的论文比较。

---

**追问：disgust 这么低，你觉得有办法提升吗？**

【应对回答】  
有三条路径：第一，数据层面，用数据增强中的 CutMix 或 MixUp 生成更多 disgust 相关的混合样本；第二，损失函数层面，用 Focal Loss 替代交叉熵，让模型更关注难分类样本（disgust 的样本很难、happy 的很简单，Focal Loss 会自动提升难样本的权重）；第三，多任务学习——同时学习表情分类和人脸关键点检测，因为 disgust 的面部动作单元（AU4、AU9、AU17）有独特的关键点位移模式，关键点信息可能帮助区分 disgust 和 anger。三条路径中，Focal Loss 是最容易实现的，可能只需要改一行代码。

---

## Q46：混淆矩阵中哪些类别之间最容易混淆？

**追问：你试过用什么方法缓解这些混淆吗？**

【应对回答】  
试过两点：一是通过数据增强，对 disgust 和 fear 的样本做更大的变换幅度，让模型看到更多变体；二是通过概率校准给容易混淆的类别设置独立的置信度阈值（如 disgust 设 0.30 较低阈值，happy 设 0.48 较高阈值），减少因阈值一刀切导致的误判。但更本质的解决方案可能需要引入额外的监督信号——比如在训练时加入"表情相似度"的知识蒸馏，告诉模型 disgust 和 anger 虽然不同、但比其他类更接近。这超出了我当前实现的范围，但可以作为未来的优化方向。

---

**追问：有没有看过被误分类的具体图片？是模型的问题还是标注的问题？**

【应对回答】  
看过。我用 `audit_model_errors.py` 脚本可视化了一批最高置信度的错误预测。发现大约 60% 的情况确实是模型判断失误——比如嘴微张的 sad 被误判为 neutral（确实很微妙）；约 20% 是标注本身有争议——比如一张标注为 anger 的图片看起来更像 disgust，这种即使人类标注者也难以达成一致；还有约 20% 是图片质量问题——严重模糊、过度曝光、或人脸占比太小。这些错误分析帮助我理解了模型的上限天花板在哪——大约有 10–15% 的"固有歧义"是无法靠改进模型搞定的。

---

## Q47：你用了 TTA（测试时增强）吗？

**追问：为什么不用多尺度 TTA？**

【应对回答】  
多尺度 TTA（如 0.8×、1.0×、1.2× 三种缩放分别推理取平均）确实能进一步提升精度，但推理时间变为 3 倍，对实时推理场景来说代价太高。水平翻转 TTA 只用 2 倍推理时间换取 0.5–1% Macro-F1 提升，性价比尚可。多尺度 TTA 在模型竞赛或离线评估中是常用手段，但在我的场景中优先级不如保持实时帧率。而且 MobileNetV3 的 224×224 输入已经固定，多尺度缩放理论上要配合特征金字塔才能发挥最大效果，单走多尺度测试增强的收益不如预期。

---

**追问：其他方向的翻转（垂直）有用吗？**

【应对回答】  
没有用。垂直翻转对人脸表情识别不仅没有帮助，还可能有害。因为人脸不会倒过来——垂直翻转后，嘴巴在上、眼睛在下，完全不符合人类的正常姿势。虽然在某些极端视角（如仰头看天花板）下人脸可能接近倒置，但 YuNet 基本检测不到这种姿态的人脸，所以垂直翻转的数据增强和处理都不适用。水平翻转之所以有效，是因为人脸有天然的大致左右对称性。这是需要根据任务特点判断的，不能无脑加增强。

---

## Q48：你这个项目最大的创新点是什么？

**追问：三阶段训练是你自己想的吗？有参考哪些论文？**

【应对回答】  
三阶段训练思路不是完全原创的，是参考了半监督学习领域的经典范式（Self-Training + Iterative Refinement），并结合自己项目的实际情况设计的。主要参考论文包括：Lee (2013) "Pseudo-Label" 提出了基本的伪标签方法；Xie et al. (2020) "Self-Training with Noisy Student" 展示了迭代伪标签在大规模数据上的有效性；Sohn et al. (2020) "FixMatch" 提供了置信度阈值和强弱增强结合的思路。我的贡献是把这些方法适配到一个实际的边缘部署场景中，并验证了在三阶段逐步精炼的范式下，MobileNetV3 这类轻量模型也能受益于半监督训练。

---

**追问：如果只说一个创新点，你选哪个？**

【应对回答】  
我选择"将三阶段半监督训练与边缘部署全链路结合"这个整体方案作为创新点。单独看三阶段训练，学术上不新；单独看 MobileNetV3 部署到树莓派，工程上也不新。但把两者串联起来——用半监督策略在有限标注数据上训练一个足够好的轻量模型，再通过量化导出部署到百元级设备上跑实时——这种"从数据到上线"的端到端工程实践，在本科毕设层面是比较少见的。它展示的不仅是算法能力，更是系统集成和工程落地能力。

---

## Q49：你的项目有哪些不足之处？

**追问：你说的数据合成具体是指什么？GAN 生成样本？**

【应对回答】  
对，一种思路是用条件 GAN（如 StarGAN 或 Fader Networks）生成特定表情的人脸图片，特别是为 disgust 和 fear 这类 minority class 扩充训练数据。另一种更轻量的是用 MixUp/CutMix 做样本混合——比如把一张 anger 的嘴巴区域贴到另一张 neutral 的脸上，生成"半怒"的训练样本。但 GAN 生成的样本质量参差不齐，且需要额外训练 GAN 模型；MixUp 虽然简单但生成的人脸可能不太自然。这需要仔细验证生成样本对下游任务的影响，不是"生成了就一定有用"。

---

**追问：为什么不直接用 RAF-DB 或 AffectNet 这些更大更全的数据集？**

【应对回答】  
RAF-DB 和 AffectNet 确实更大更全，标注质量也更高。没用它们的原因比较实际：第一，时间窗口。我项目早期选定了 FER2013Plus 和 MMAFEDB 后就沿着这个方向做了数据清洗和实验，中途切换到 RAF-DB 意味着重做数据处理和部分实验；第二，数据下载和存储限制。RAF-DB 需要申请并签署协议，AffectNet 超过 100GB，下载和存储都有实际困难。但如果重来一次，我会优先考虑使用 AffectNet 的 7 分类子集（约 28 万张），因为它既有实验室数据也有 in-the-wild 数据，标注也更可靠。

---

## Q50：如果现场演示时系统出问题了

**追问：你能现场诊断一下是什么问题吗？**

【应对回答】  
可以试试几招快速诊断：第一步，检查摄像头是否被识别——`ls /dev/video*` 看设备节点是否存在，或者用 `cv2.VideoCapture(0).isOpened()` 测试；第二步，单独测试 YuNet 检测——加载模型后用一张静态图片跑检测，看是否正常出框；第三步，单独测试 TFLite 推理——用一张 224×224 的随机张量跑一次推理，看是否正常输出 7 维 logits。如果这三步都没问题，问题可能在多线程调度或内存不足。如果时间允许，我会简化 Config（关掉校准、关掉跟踪、单线程），逐步加回功能来定位故障点。这种分模块隔离诊断的思路也是系统性 debug 的基本方法。

---

**追问：如果是因为你的代码有 bug 呢？**

【应对回答】  
坦率承认并分析。如果是已知的 bug（比如某个 Config 参数在特定条件下会触发异常），我会直接说明已知的限制和解法；如果是未知的 bug，我会现场尝试修改代码（比如注释掉可疑模块、加 try-except 降级处理），同时解释为什么选择这个临时方案。但更重要的是，我会在答辩前确保核心流程（检测→推理→显示）的健壮性——这一条链路代码相对简单且经过了充分测试，出 bug 的概率很低。出问题更可能是环境因素（光照、驱动、摄像头兼容性），而不是代码逻辑本身。答辩专家更看重的是应变能力和分析思路，而不是"代码零 bug"。

---

## 附录：追问覆盖率统计

| 问题编号 | 追问数 | 问题编号 | 追问数 | 问题编号 | 追问数 |
|---------|-------|---------|-------|---------|-------|
| Q1 | 2 | Q18 | 2 | Q35 | 3 |
| Q2 | 2 | Q19 | 2 | Q36 | 2 |
| Q3 | 2 | Q20 | 3 | Q37 | 2 |
| Q4 | 2 | Q21 | 2 | Q38 | 2 |
| Q5 | 2 | Q22 | 2 | Q39 | 2 |
| Q6 | 2 | Q23 | 2 | Q40 | 2 |
| Q7 | 2 | Q24 | 2 | Q41 | 2 |
| Q8 | 2 | Q25 | 2 | Q42 | 2 |
| Q9 | 2 | Q26 | 2 | Q43 | 2 |
| Q10 | 3 | Q27 | 2 | Q44 | 2 |
| Q11 | 2 | Q28 | 2 | Q45 | 2 |
| Q12 | 2 | Q29 | 2 | Q46 | 2 |
| Q13 | 2 | Q30 | 2 | Q47 | 2 |
| Q14 | 2 | Q31 | 2 | Q48 | 2 |
| Q15 | 2 | Q32 | 2 | Q49 | 2 |
| Q16 | 2 | Q33 | 2 | Q50 | 2 |
| Q17 | 2 | Q34 | 2 | | |

**总计：50 个问题 × 平均 2.14 条追问 = 107 条追问，全部覆盖。**

---

> **使用建议**：  
> 1. 本手册配合《答辩专家问题与标准回答手册》一起使用。先熟记主回答，再浏览对应追问。  
> 2. 部分追问涉及具体数字——请确保这些数字与你 PPT 和论文中的数据一致。  
> 3. 代码层面的细节（如函数名、文件路径、类名）在原手册中已提及的，本手册不再重复。  
> 4. 对于你自己没有实际测过的数字（如 INT8 量化损失），回答中已标为"初步测试"或"了解但未实测"，答辩时请注意措辞不要过度承诺。

---

*本手册所有回答均基于项目 `fer-pi5` 的实际代码、配置文件与实验日志生成。祝答辩顺利！*
